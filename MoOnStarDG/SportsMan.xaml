<Window x:Class="MoOnStarDG.SportsMan"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MoOnStarDG"
        mc:Ignorable="d"
        Title="SportsMan" Height="450" Width="1000">
    <Grid Background="#FFD2A782">
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="10"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Label Grid.Row="0" Grid.ColumnSpan="3" Content="Список спортсменов и тренировок" 
               FontWeight="Bold" FontSize="20" HorizontalAlignment="Center"/>

        <StackPanel Grid.Row="1" Grid.Column="0">
            <Label Content="Спортсмены:" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>
            <DataGrid x:Name="SportsmenGrid" 
                     AutoGenerateColumns="False" 
                     CanUserAddRows="False"
                     Background="White"
                     Height="270">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Имя" Binding="{Binding Name}" Width="100"/>
                    <DataGridTextColumn Header="Фамилия" Binding="{Binding FirstName}" Width="120"/>
                    <DataGridTextColumn Header="Дата рождения" Binding="{Binding DdataBirsDay}" Width="110"/>
                    <DataGridTextColumn Header="Категория" Binding="{Binding IdCategoryNavigation.Title}" Width="100"/>
                    <DataGridTextColumn Header="Уровень" Binding="{Binding IdLevelNavigation.Title}" Width="100"/>
                </DataGrid.Columns>
            </DataGrid>

        </StackPanel>

        <Rectangle Grid.Row="1" Grid.Column="1" Fill="Gray" Width="2" HorizontalAlignment="Center"/>

        <StackPanel Grid.Row="1" Grid.Column="2">
            <Label Content="Тренировки:" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>
            <DataGrid x:Name="TrainingsGrid" 
                     AutoGenerateColumns="False" 
                     CanUserAddRows="False"
                     Background="White"
                     Height="270">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Название" Binding="{Binding Title}" Width="150"/>
                    <DataGridTextColumn Header="Дата" Binding="{Binding TrainingDate}" Width="100"/>
                    <DataGridTextColumn Header="Длительность" Binding="{Binding IdTrainingTime}" Width="150"/>
                    <DataGridTextColumn Header="Тип" Binding="{Binding Type.Title}" Width="100"/>

                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>

        <Button Grid.Row="2" Grid.ColumnSpan="3" Content="Назад в главное меню" 
               Click="BackToMainBtn_Click" Width="150" Height="30" 
               HorizontalAlignment="Center" Background="#FFE6DBA9"/>
    </Grid>
</Window>